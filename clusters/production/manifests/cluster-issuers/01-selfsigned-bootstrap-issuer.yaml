# Step 1 of 3: a bare selfSigned issuer whose only job is to sign the root
# CA certificate below. Nothing else should ever reference this issuer
# directly.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-bootstrap
spec:
  selfSigned: {}
